import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Yash R. Mankar - Software Engineer & Forensic Analyst",
  description:
    "Welcome to my personal website! I'm Yash R. Mankar, a passionate software engineer and forensic analyst. Here, you'll find insights into my work, projects, and expertise in software development and digital forensics. Explore my portfolio, read about my experiences, and feel free to connect with me for collaborations or inquiries.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
